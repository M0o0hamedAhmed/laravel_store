<button
    type="button"
    class="btn btn-info bg-teal addBtn"
    data-toggle="modal"
    data-target="#modal-open">{{($slot != "") ? $slot : "Add"}} </button>


{{--to extend this copy following--}}
{{--<x-forms.buttons.add>Slot</x-forms.buttons.add>--}}
